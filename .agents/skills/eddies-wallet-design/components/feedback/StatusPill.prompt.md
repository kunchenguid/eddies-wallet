The status vocabulary for every money event: Recorded, Waiting to sync, Not recorded, Draft on this iPad. Always icon + text — never color alone, per product requirement.

```jsx
<StatusPill status="pending" />
```

Use exactly these four states; don't invent new sync-status wording elsewhere.
