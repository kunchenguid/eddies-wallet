The local PIN gate for entering Parent mode on a shared iPad. A wrong PIN should leave the app in the current mode without revealing parent content (see readme for the exact rule).

```jsx
<PinPad value={pin} onChange={setPin} />
```
