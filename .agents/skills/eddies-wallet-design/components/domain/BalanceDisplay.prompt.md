The wallet's hero number. Always pairs the balance with the persistent virtual/nonredeemable notice — never show the balance alone.

```jsx
<BalanceDisplay amount="24.00" lastUpdated="2 min ago" />
```
