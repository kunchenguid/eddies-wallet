The always-visible role indicator/switcher on a shared iPad. Selecting Parent must trigger the PIN gate (see PinPad) before revealing parent controls.

```jsx
<RoleSwitch role={role} onChange={setRole} />
```
