One row in the wallet's Recent Activity list, shown directly on the wallet (no separate activity tab per PRD).

```jsx
<ActivityRow type="allowance" title="Allowance" subtitle="Jul 21 · Weekly" amount="10.00" onClick={openDetails} />
```
